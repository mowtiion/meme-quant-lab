# Public read-only RPC by default. Keep credentials outside Git and chat.
SOLANA_RPC_URL=https://api.mainnet-beta.solana.com
# Optional later; neither service is called by the default pipeline.
HELIUS_API_KEY=
BITQUERY_ACCESS_TOKEN=
